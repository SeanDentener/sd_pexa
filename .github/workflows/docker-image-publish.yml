name: Docker Image Publish

on:
  push:
    branches:
    - main

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Azure Container Registry Login
      uses: Azure/docker-login@v2
      with:
        # Container registry username
        username: ${{ secrets.SAMPLEAPP_ACR_USERNAME }}
        # Container registry password
        password: ${{ secrets.SAMPLEAPP_ACR_PASSWORD }}
        # Container registry server url
        login-server: github.com

    - name: Lowercase the repo name
      run: echo "REPO=${GITHUB_REPOSITORY,,}" >>${GITHUB_ENV}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v4
      with:
          push: true
          tags: 'github.com/seandentener:${{ github.sha }}'
          file: ./WebApp.Dockerfile




    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@85270a1854658d167ab239bce43949edb336fa7c
      with:
        app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        images: 'github.com/seandentener:${{ github.sha }}'



     # - uses: actions/checkout@v3
    # - name: Build the Docker image
    #   run:         
    #     docker build . --file WebApp.Dockerfile --tag api.github.com/repos/seandentener/sd_pexa:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER;
    #     docker tag github.com/seandentener/sd_pexa:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER github.com/seandentener:latest;
        # docker push api.github.com/repos/seandentener:$(date +'%Y-%m-%d')_$GITHUB_RUN_NUMBER;
        # docker push api.github.com/repos/seandentener/sd_pexa:latest;  